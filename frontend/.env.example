# Frontend only. Copy to frontend/.env.local.
# Never put GROQ_API_KEY or other server-side secrets in NEXT_PUBLIC_* variables.
# Change this URL only when FastAPI is hosted on another origin.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
